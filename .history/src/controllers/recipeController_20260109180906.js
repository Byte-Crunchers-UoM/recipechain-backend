import { supabase } from '../config/supabase.js';

export const addRecipe = async (req, res, next ) => {
    try{
        const{
            title,
            description,
            category,
            
        }
    }
}